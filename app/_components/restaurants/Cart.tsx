import { RestaurantDetailProps } from '@/app/_types/restaurants/detailProps';
import React from 'react';

/**
 * 장바구니
 */
export default function Cart({ restaurant }: RestaurantDetailProps) {
  return <div>Cart</div>;
}
